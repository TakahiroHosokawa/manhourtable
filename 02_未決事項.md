# 02 未決事項

> 工数管理表アプリ 要件定義 v0.11 ／ 更新：2026-09-11
> これまで仮置きとしていた仕様（旧★マーク）は、すべて決定事項として確定しました。

## 1. 押さえておくべき前提（決定済みだが、影響が大きいもの）

### 台あたりと、台に割れない行

「全行1台あたり」と「間接作業を行にする」は両立しない。朝礼・清掃・日常点検は日あたりで、台数が変われば台あたりの値も変わるため。

**対応**：単位列（台／便／日）で使い分ける。台以外の行は台あたり集計に混ぜず別枠に出す。台あたりへの換算は、日産台数・便数が決まる第2段で行う。

### 員数が無いことの帰結

部品表 CSV は 製品型番・部品品番・部品名 の3列で、**設計員数が無い**。したがって台あたり必要数は「紐づけた使用数の合計」でしか出せない。

**帰結**：**工数表の紐づけ漏れが、そのままキットの欠品になる。**防波堤は未割付の一覧（C33）だけなので、「未割付ゼロで発行」を運用として決める必要がある（06_統制運用 O18）。員数ベースの照合が要るなら、CSV に員数列を足すのが最短。

### 使用数を「1台あたり」に統一した代償

数量（箇所数）が設計変更で変わると、使用数も直さないとずれる（4本×153箇所＝612 → 箇所数が160なら 640）。

**対応**：使用数の入力欄に「×数量」ボタン（F58）。数量を変更した小工程に使用部品があれば警告（F57）。

### 順序と先行作業

並行が多く班が入れ替わるため、**順序は表示のためのもの**。計算上の拘束は先行作業。同じ班・同じ中工程の中は機械が順序どおり直列と仮定し、人が入力するのは班・中工程をまたぐ依存だけ（D56）。

**帰結**：先行作業の入力・維持の担当を決める必要がある（O22）。未入力だと第2段の山積みが成立しない。

### ステーション単位の所要時間

班がステーションを移動するため、「大工程の所要＝最長班」は正確ではない。**第1段では参考値**とし、正確な値は第2段の山積みで出す。

---

## 2. 決定事項（旧 未決事項）

### 運用

| No | 内容 | 決定内容 |
|---|---|---|
| Q06 | 委託先への渡し方 | 閲覧させず、出力（スプレッドシート）を配布 |
| Q07 | 現場端末は iPad か | 閲覧はスマホ／iPad、編集は PC |
| Q08 | 所有者・引き継ぎ（GAS プロジェクトと配信アカウント） | 共有ドライブに置き、管理責任者を決める |
| Q09 | シミュレータ側の工程表と大工程名を揃えるか | 揃える（第2段の連携の前提） |
| Q19 | マスタ管理責任者（部門・人） | 共有ドライブ管理者が兼任 |
| Q20 | 版確定のタイミングの運用ルール | 生産ペース変更・レイアウト変更・改善反映の節目 |
| Q21 | 出荷工程（積み込み・固縛・養生）の時間の出所。現行 Excel に無く未計測 | 観測して登録。実装とは独立に進められるデータ整備タスク |

### データの持ち方

| No | 内容 | 決定内容 |
|---|---|---|
| Q10 | 目標値の項目範囲 | 目標正味時間・目標人数のみ。廃止は目標正味時間＝0 |
| Q11 | 中工程・組立ブロックの順序変更方法 | 順序番号で指定。ドラッグは小工程 |
| Q12 | 数量の参照形式 | 固定／パラメータ×係数／部品の使用数 |
| Q15 | ID の採番 | ブロック＝製品コード-B-連番、中工程＝-M-、小工程＝-E-。再利用しない |
| Q16 | 委託先提示の項目 | 版に 提示日・提示先・合意状況。第1段は入力のみ |
| Q17 | 変更履歴の粒度 | 項目単位。順序変更は「順序」項目の変更として1件 |
| Q22 | 段取りと間接作業を区分として分けるか | 分ける |
| Q63 | 大工程（ステーション）は型式をまたいで共通でよいか | 共通。同じラインに複数型式が流れる前提 |
| Q64 | ステーション間を移動する小工程の大工程の付け方 | 開始するステーションを付け、移動先は備考 |
| Q66 | 組立ブロックは製品ごとか、型式共通か | 製品ごと（型式で順序が変わるため）。突き合わせは名前で |
| Q67 | 中工程を1つしか持たない組立ブロック（現行18種のうち8種） | そのまま。ブロック名と中工程名が同じでも構わない |

### 用語

| No | 内容 | 決定内容 |
|---|---|---|
| **Q61** | **組立ブロックの呼び名。**現場が「大工程」をステーションに使っているため別名が要る | **組立ブロック** で決定 |
| Q18 | その他の用語 | 大工程／中工程／小工程 は現場の呼び方どおりで決定 |

### 部品

| No | 内容 | 決定内容 |
|---|---|---|
| Q27 | CSV で同一 製品型番×品番 が複数行あるか（行数＝員数か） | 1行と仮定。重複は取込時に警告して1件にまとめる |
| Q28 | 同じ品番で部品名が違う場合 | 最初の値を採用し、不一致を一覧で警告 |
| Q29 | 部品区分をどこまで付けるか | 使用部品として紐づけた品番だけ |
| Q30 | 荷姿・入数・置場・搬入条件をどこまで入力するか | 紐づけた品番だけ。未入力を許す |
| Q31 | 荷姿等の正本をこのアプリにしてよいか | このアプリを正本、シミュレータへ片方向出力 |
| Q32 | CSV の文字コード・区切り・ヘッダ行 | UTF-8／カンマ／ヘッダあり |
| Q33 | 取込で消えた行の扱い | 自動で無効化せず、候補として提示 |

### キット

| No | 内容 | 決定内容 |
|---|---|---|
| Q47 | 数量変更時に使用数の見直し警告を出すか | 出す |
| Q48 | 除外する部品区分の設定単位 | アプリ全体（設定シート） |
| Q49 | キット表の出力形式 | 画面＋スプレッドシート出力。印刷用の帳票は後段 |
| Q50 | キットのマスタ（キットコード・箱番号・ラベル） | 第1段では持たない |
| Q51 | 入数で割り切れない端数 | 箱数は切り上げ、端数はバラとして表示 |
| Q52 | 中工程別キットで同じ品番が複数の中工程に出る場合 | 中工程ごとに分けて出す（合算しない） |

### 観測

| No | 内容 | 仮置き |
|---|---|---|
| Q37 | 確度は3値（未観測／暫定／確定）でよいか | よい ★ |
| Q38 | 代表値に何を使うか | 平均と中央値を表示し、人が決める ★ |
| Q39 | レンジ比の警告しきい値 | 1.5 以上。設定で変更可 ★ |
| Q40 | 版明細に生データを写すか | 写さない。代表値・確度・ばらつき要約のみ ★ |
| Q41 | 機械時間も観測対象にするか | する ★ |
| Q58 | 観測数量の既定値 | 1 ★ |

### 設備・作業方式

| No | 内容 | 仮置き |
|---|---|---|
| Q53 | 設備の粒度（電動ドライバ・トルクレンチなどの工具、治具まで登録するか） | 山積みで取り合いになるものだけ ★ |
| Q54 | 可搬設備の設置大工程 | 設置大工程は任意。可搬は空欄 ★ |
| Q55 | 占有方式「共有」の使いどころ | 台数の制約にならないもの（電源・エア・作業台）。記録のみ ★ |
| Q56 | 作業方式と人数・設備・区分の整合ルール | 04_計算定義 の表のとおり ★ |
| Q57 | 中断可否の既定値 | 中断可 ★ |

### 複製・差分・第2段

| No | 内容 | 仮置き |
|---|---|---|
| Q42 | 複製時に使用部品の品番をそのまま引き継ぐか | 引き継ぐ。型式で変わる分は突き合わせ（C22）で拾う ★ |
| Q43 | 派生元は直近1つだけでよいか | よい。たどってルートを出す ★ |
| Q44 | 差分比較の突き合わせ鍵の優先順 | ①派生元 ②ブロック名＋中工程名＋小工程名 ③未対応 ★ |
| Q45 | 系譜でつながった小工程への改善の横展開の印 | 出す（推奨）★ |
| Q46 | 混流の「型式の組合せ」の持ち方 | 計画パラメータ側に版の組合せを持つ。第2段で定義 ★ |
| Q60 | 作業者のスキル・資格 | 今回は見送り。担当職種で代用。第2段で必要になったら追加 ★ |
| Q65 | 工数表の既定の見え方 | 作業の木（ブロック→中工程→小工程）。大工程×班に切替可 ★ |

---

## 3. 実装前に決めたいもの（優先順）

| 順 | 項目 | 理由 |
|---|---|---|
| 1 | Q61 組立ブロックの呼び名 | 画面文言と用語ヘルプ。後から変えると全画面の置換 |
| 2 | Q53 設備の粒度 | 設備台帳の維持コストと山積みの精度に直結 |
| 3 | Q29・Q30 部品区分と荷姿の入力範囲 | データ整備の作業量が変わる |

他の★は `00_定数.gs` に集約されるため、仮置きのままで実装を進めてよい。
