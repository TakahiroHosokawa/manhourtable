# 00 概要と決定事項

> 工数管理表アプリ 要件定義 v0.11
> 更新：2026-09-11
> 旧仕様書にあった未決事項はすべて決定事項として確定しました。

## ファイル構成

| ファイル | 内容 |
|---|---|
| 00_概要と決定事項.md | この文書。目的・範囲・構造・決定事項の一覧 |
| 01_用語集.md | 用語の定義と現行 Excel との対応 |
| 02_未決事項.md | 決定済みの要点と、未決事項の一覧 |
| 03_データ構造とシート定義.md | シート一覧・関係・全列の定義 |
| 04_計算定義.md | C01〜C44 と作業方式の整合ルール |
| 05_画面と機能要件.md | S01〜S17 と F01〜F69 |
| 06_統制運用.md | 権限・配信・バックアップ・維持の運用 |

---

## 概要

| 項目 | 内容 |
|---|---|
| 対象 | MegaPower 組立（MP2500 ほか）の要素作業標準工数。複数型式に対応 |
| 目的 | 要素作業の標準工数を製品×版で整備・改訂管理する。使用部品・観測値・設備を同じ場所に持ち、部品キット化と人員試算の土台にする |
| 範囲 | コンテナ搬入 〜 トラックへの積み込み・固縛完了まで |
| 第1段 | 標準工数マスタ＋部品（取込・紐付け・逆引き・突き合わせ・キット表）＋観測記録とばらつき＋製品の複製・型式間の差分比較＋設備と作業方式 |
| 第2段 | 計画パラメータ・タクト・必要人員の自動山積み・BOM差分による新型式の見積り・シミュレータ向け出力 |
| 基盤 | GAS Web アプリ（HTML Service）＋ Google スプレッドシート。Workspace ログイン。「自分として実行」で配信 |
| 利用者 | **入力は限られた担当者。閲覧は社内の他部門も**。ロール制御はせず、記録で担保。日本語のみ |
| 端末 | 編集は PC 前提。スマホ／iPad は閲覧 |
| 初期データ | 工数表は取り込まない（1から登録）。部品表のみ CSV から取り込む |
| 委託先 | 第1段は社内計画用。確定者・委託先提示・合意の項目は DB に用意し、画面は後段 |

**この製品の特徴（設計の前提）**：多数の作業が並行して進み、作業班は小工程ごとに入れ替わり、ステーションを貫通して実施される作業もある。したがって「作業の木」と「ステーション」は独立した2つの軸として扱う。

---

## 構造

```
製品（型式）
 └ 組立ブロック       何を組み付けるか（消火器類取付、MD挿入…）／製品内で順序
     └ 中工程          作業のまとまり（Stat-X本体固定、MD挿入…）／ブロック内で順序
         └ 小工程      時間を持つ最小単位（リフター上昇、ジグにセット…）／中工程内で順序
              ├ 大工程（作業ステーション）  ← 製品共通のマスタを参照
              ├ 作業班                      ← 製品共通のマスタを参照
              ├ 区分／作業方式／単位／時間方式／正味時間／数量／人数／中断可否
              ├ 使用部品（n件）／使用設備（n件）／観測記録（n件）
              └ 先行作業（他の小工程）
```

> **最も間違えやすい点**：**大工程（ステーション）は作業の木の一部ではない。**小工程がどこで行われるかを示す属性であり、中工程の親ではない。現場では 大工程＝ステーション、中工程＝作業のまとまり、小工程＝要素作業 と呼んでいるため呼称はそのままにするが、画面上「大工程」を選んでも中工程の親にはならない。

---

## 決定事項

| No | 項目 | 決定内容 |
|---|---|---|
| D01 | 目的 | 標準工数マスタの整備・改訂管理を第1段とする |
| D02 | 実績の出所 | 社内で計測（動画・ストップウォッチ）。日次実績ではなく時間観測 |
| D03 | 利用者 | 入力は限られた担当者。閲覧は他部門も。委託先は使わない |
| D04 | 基盤 | GAS Web アプリ＋スプレッドシート。GitHub Pages は不採用 |
| D05 | 版管理 | 行ごとの変更履歴＋節目で版スナップショット（手動の版確定） |
| D06 | 編集権限 | ロール制御はしない。無効化・履歴・確定者記録で担保 |
| D07 | 表示言語 | 日本語のみ |
| D08 | 時間の扱い | 分担作業／同時作業＋拘束人数。人数0は機械・自動 |
| D09 | 並行作業 | 作業班で表す。班内は直列、班同士は並行 |
| D10 | 用語 | 製造で使う専門用語を使う。現行シートの言葉は残す |
| D11 | 計画パラメータ等 | 第1段に含めないが第2段で実装。DB の枠は確保 |
| D12 | 初期データ | 工数表は取り込まない。部品表は例外（D28） |
| D13 | 順序入替 | ドラッグ＆ドロップ＋上下ボタン |
| D14 | 無効化・改訂理由 | 削除は無効フラグ。版確定時は改訂理由・確定者を必須 |
| D15 | 現行と確定版 | 一覧は現行。集計と出力は既定で最新確定版（切替可） |
| D16 | マスタの統制 | 大工程・作業班・設備・担当・部品区分は台帳／選択肢で管理し、行では選ぶだけ |
| D17 | 用途と権威 | まずは社内計画用。委託先提示は列のみ用意 |
| D18 | 標準の性格 | 現状値と目標値の2値を行に持つ |
| D19 | 数量の出所 | 製品構成パラメータを設定でき、行から参照する |
| D20 | 階層 | **作業の木は 組立ブロック → 中工程 → 小工程 の3段。大工程は木の一部ではなく小工程の属性** |
| D21 | 範囲 | コンテナ搬入 〜 トラック積み込み・固縛完了 |
| D22 | 区分 | 実作業／付随作業／加工作業／運搬／手待ち／段取り／間接作業。間接作業も行にする |
| D23 | 余裕率 | 疲労・用達・職場余裕のみ（既定15%）。段取りは行にする |
| D24 | レイティング | 第1段では補正しない。観測者を記録に残す |
| D25 | 単位 | 原則「台あたり」。単位列（台／便／日）を持ち、台以外は別枠集計 |
| D26 | 部品紐付けの目的 | ①欠品・設計変更の影響検索 ②機種差の工数 ③搬入計画（第2段）④キット化 |
| D27 | 紐付けの単位 | 小工程。多対多（中間シート「使用部品」） |
| D28 | 部品の正本 | CSV 部品表（A:製品型番／B:部品品番／C:部品名）を取り込む |
| D29 | 部品区分 | 持つ。CSV に無いのでアプリ側で付与 |
| D30 | リビジョン | 持たない。その時点の品番を文字で持つ |
| D31 | 荷姿・入数・置場・搬入条件 | このアプリに持ち、正本もこのアプリ。シミュレータへは片方向出力 |
| D32 | 数量との関係 | 数量方式に「部品の使用数」を追加。複数品番のときは数量基準フラグで1つ選ぶ |
| D33 | 観測記録 | 第1段に持つ。測定値を小工程に紐づけて登録し、標準の根拠にする |
| D34 | ばらつきと確度 | サンプル数・最小・最大を自動表示し、確度（未観測／暫定／確定）も持つ |
| D35 | 標準値の決め方 | 観測値から自動で上書きしない。参考値を表示し、人が確定入力する |
| D36 | 丸め | 丸め単位を製品の設定で持つ（秒／5秒／10秒／分）。強制はしない |
| D37 | 製品の複製 | 第1段。ブロック・中工程・小工程・使用部品・使用設備を複製。版・履歴・観測記録は複製しない |
| D38 | 派生元（系譜） | 複製時に派生元のID を残す |
| D39 | 型式間の差分比較 | 第1段 |
| D40 | 複製後の確度 ★ | 「暫定」にする。代表値の出所は「既存資料（派生元）」 |
| D41 | 使用数の単位 | すべて「1台あたり」に統一 |
| D42 | キット表の除外 | 消耗品・常備品は部品区分で除外。供給方式の列は持たない |
| D43 | キットの単位 | 大工程別・組立ブロック別・中工程別。キットのマスタは持たない ★ |
| D44 | 員数 | CSV に追加できないため使用数の合計で代用。未割付を発行の関門に |
| D45 | 人員試算 | **(B) 自動山積み**。第2段で実装、項目は第1段で入力 |
| D46 | 作業方式 | 区分とは別の軸として持つ。6区分 |
| D47 | 設備 | 台帳と多対多（必要台数・占有方式）。型式をまたいで共通 |
| D48 | 大工程の最大同時人数 | 大工程に持つ |
| D49 | 中断可否 | 小工程に項目だけ持つ。計算は第2段 |
| D50 | 観測数量 | 観測記録に持ち、換算値（観測値÷観測数量）で集計 |
| D51 | Q36 | 取り下げ（MD挿入の測定と工数表の差は例示であり、実データの矛盾ではない） |
| D52 | ステーションと班の位置 | 中工程ではなく**小工程**に付ける |
| D53 | 中工程の役割 | 作業のまとまり。ステーションにも班にも縛られない |
| D54 | 組立ブロック | 中工程の親として正式な階層にする。現行 A列の値が入る |
| D55 | 順序の導出 | 班の作業列は ブロック順 → 中工程順 → 小工程順。順序は表示のためのもの |
| D56 | 先行作業の入力範囲 | 同じ班・同じ中工程の中は「書いた順に直列」と機械が仮定する。**班・中工程をまたぐ依存だけ人が入力** |
| D57 | 用語 | 大工程／中工程／小工程 は現場の呼び方どおり。そのまま画面に使う |
| **D58** | **同時編集（v0.10）** | **入力者が限られるため、項目単位のマージは実装しない。**LockService で直列化し、保存時に更新日時を突き合わせて他者の更新があれば中断して知らせる |
| **D59** | **組立ブロックの管理（v0.11）** | 組立ブロックはアプリで登録・編集する。**S01 製品設定に一覧を置き**、工数表のツリー上でも追加・改名・順序変更できる。新規作成時は**他型式の既存ブロック名を候補表示**し、**改名時はその名前で突き合わせている型式の件数を警告**する |

---

## バージョン履歴

| 版 | 主な変更 |
|---|---|
| v0.1 | 標準工数マスタの骨格（製品・工程・作業・作業内訳・版・履歴） |
| v0.2 | 範囲を出荷まで拡張。段取り・間接作業を区分に追加。余裕率を限定。単位列 |
| v0.3 | 使用部品（品番の紐付け）、部品区分、CSV 取込 |
| v0.4 | 観測記録を第1段に前倒し。ばらつきと確度 |
| v0.5 | 製品の複製・型式間の差分比較・派生元（系譜）。全文版に統合 |
| v0.6 | キット表。使用数を「1台あたり」に統一（定義の不整合を修正） |
| v0.7 | 人員試算を(B)自動山積みに決定。作業方式・設備・使用設備・中断可否・観測数量 |
| v0.8 | 階層を 大工程／中工程／小工程 に再定義。ステーションと班を小工程へ |
| v0.9 | 組立ブロックを中工程の親として階層化。先行作業の入力範囲を確定 |
| v0.10 | 入力者に関する訂正（同時編集マージを不要に）。文書を7分割 |
| **v0.11** | 組立ブロックの一覧を S01 に追加。名前の統一を助ける入力補助と改名警告 |
